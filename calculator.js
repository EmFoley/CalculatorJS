// on click of button, add value to value array and display
// in results window
// on click of operator, operate

// repeat until = is hit, then display final result 

// C once clears the current value array, C twice clears all the arrays

function collectValues() {
 var enteredIn = [];
 var valEntered = document.getElementById
 enteredIn.push(value);
 console.log(enteredIn);
};

var key = document.getElementById('keys');
key.addEventListener("click", collectValues, false);
